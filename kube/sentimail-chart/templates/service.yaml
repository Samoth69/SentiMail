apiVersion: v1
kind: Service
metadata:
  name: {{ include "sentimail.fullname" . }}
  labels:
    {{- include "sentimail.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sentimail.selectorLabels" . | nindent 4 }}
