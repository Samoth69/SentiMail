- name: deploy app
  k8s:
    apply: true
    namespace: sentimail-dev
    template: "{{ lookup('file', item) | to_yaml }}"
  with_items:
    - ../kube/sentimail-manual/sentimail/backend.yml
    - ../kube/sentimail-manual/sentimail/ms-metadata.yml